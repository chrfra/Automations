---
phase: 01-foundation
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - server/vitest.config.ts
  - server/tests/crypto.test.ts
  - server/tests/proxy.test.ts
  - server/tests/execute.test.ts
  - server/tests/startup.test.ts
autonomous: true
requirements:
  - REQ-01
  - REQ-02
  - REQ-03
  - REQ-04
  - REQ-05
  - REQ-06

must_haves:
  truths:
    - "Test suite runs and produces pass/fail output with a single command"
    - "All 6 requirement behaviors have test coverage stubs that fail before implementation"
    - "Crypto round-trip test is written and ready to go red"
    - "Startup key-missing test is written and ready to go red"
    - "Proxy forwarding and 502 mapping tests are written and ready to go red"
    - "Route validation (400 cases) tests are written and ready to go red"
  artifacts:
    - path: "server/vitest.config.ts"
      provides: "Vitest configuration with path alias resolution"
    - path: "server/tests/crypto.test.ts"
      provides: "AES-256-GCM encrypt/decrypt round-trip + key load failure tests"
    - path: "server/tests/proxy.test.ts"
      provides: "Upstream call passthrough, timeout (502), connection error (502) tests"
    - path: "server/tests/execute.test.ts"
      provides: "Fastify route validation — missing URL, unsupported method, 400 responses"
    - path: "server/tests/startup.test.ts"
      provides: "Missing key file → process.exit(1) behaviour test"
  key_links:
    - from: "server/vitest.config.ts"
      to: "server/src/**"
      via: "alias resolution matching tsconfig paths"
      pattern: "resolve.*@.*src"
    - from: "server/tests/crypto.test.ts"
      to: "server/src/lib/crypto.ts"
      via: "import { encrypt, decrypt, loadEncryptionKey }"
      pattern: "import.*crypto"
    - from: "server/tests/proxy.test.ts"
      to: "server/src/lib/proxy.ts"
      via: "import { callUpstream }"
      pattern: "import.*proxy"
    - from: "server/tests/execute.test.ts"
      to: "server/src/routes/execute.ts"
      via: "Fastify inject"
      pattern: "fastify.inject"
    - from: "server/tests/startup.test.ts"
      to: "server/src/lib/crypto.ts"
      via: "import { loadEncryptionKey }"
      pattern: "import.*loadEncryptionKey"
---

<objective>
Create the Wave 0 test scaffold — Vitest configuration and all test files that will drive Phase 1 implementation.

Purpose: Nyquist compliance requires test files exist before implementation tasks run so every auto task has an `<automated>` verify command pointing at a real file. These tests go red immediately (modules don't exist yet) and turn green as implementation tasks complete.
Output: vitest.config.ts + 4 test files covering all 6 Phase 1 requirements. No production code is written here.
</objective>

<execution_context>
@/home/cf/.claude/get-shit-done/workflows/execute-plan.md
@/home/cf/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Vitest and create vitest.config.ts</name>
  <files>server/package.json, server/vitest.config.ts</files>
  <action>
    Inside the `server/` directory (which does not yet exist — create it with a minimal package.json first):

    1. Create `server/package.json` with:
       - `"name": "automations-server"`, `"version": "0.1.0"`, `"type": "module"`
       - `"scripts": { "test": "vitest run", "test:watch": "vitest" }`
       - Empty `dependencies` and `devDependencies` objects (will be populated in Plan 02)

    2. Run: `cd /home/cf/.openclaw/workspace/apps/automations/server && npm install --save-dev vitest @vitest/coverage-v8`

    3. Create `server/vitest.config.ts`:
       ```typescript
       import { defineConfig } from 'vitest/config'
       import { resolve } from 'node:path'

       export default defineConfig({
         test: {
           globals: false,
           environment: 'node',
           include: ['tests/**/*.test.ts'],
           reporter: 'dot',
         },
         resolve: {
           alias: {
             '@': resolve(__dirname, './src'),
           },
         },
       })
       ```

    The alias `@` → `./src` must match what tsconfig paths will define in Plan 02 (`@/*` → `src/*`).
  </action>
  <verify>
    <automated>cd /home/cf/.openclaw/workspace/apps/automations/server && npx vitest run --reporter=dot 2>&1 | grep -E "No test files|0 tests|passed|failed" | head -5</automated>
  </verify>
  <done>vitest runs without config errors (may report "no test files found" — that is correct at this stage since test files haven't been written yet)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write failing test files for all 6 requirements</name>
  <files>
    server/tests/crypto.test.ts,
    server/tests/proxy.test.ts,
    server/tests/execute.test.ts,
    server/tests/startup.test.ts
  </files>
  <behavior>
    crypto.test.ts:
    - encrypt(plaintext, key) → string in format "hex:hex:hex" (iv:tag:ciphertext)
    - decrypt(encrypt(plaintext, key), key) === plaintext (round-trip)
    - decrypt with wrong key throws (auth tag mismatch)
    - loadEncryptionKey() with missing file calls process.exit(1)
    - loadEncryptionKey() with 32-byte key file returns Buffer of length 32

    proxy.test.ts:
    - callUpstream with mocked fetch returning 200 → { ok: true, status: 200, headers: {...}, body: "..." }
    - callUpstream passes custom headers to fetch (REQ-03)
    - callUpstream passes body for POST (REQ-04)
    - callUpstream with TimeoutError → { ok: false, status: 502, message: includes "timed out" }
    - callUpstream with ECONNREFUSED → { ok: false, status: 502, message: includes "connection refused" }
    - callUpstream with ENOTFOUND → { ok: false, status: 502, message: includes "hostname not found" }

    execute.test.ts (uses Fastify inject — no real network):
    - POST /api/execute with missing url → 400 with plain-language error message (REQ-01)
    - POST /api/execute with unsupported method "CONNECT" → 400 (REQ-02)
    - POST /api/execute with valid payload → delegates to callUpstream (mock it) → returns upstream status (REQ-06)

    startup.test.ts:
    - loadEncryptionKey() when key file missing → process exits with code 1 (spy on process.exit)
    - loadEncryptionKey() with key shorter than 32 bytes → process exits with code 1
  </behavior>
  <action>
    Write all four test files. Since the source modules (`@/lib/crypto`, `@/lib/proxy`, `@/routes/execute`) don't exist yet, ALL tests will fail with "Cannot find module" — that is the correct RED state.

    Use `vi.mock` / `vi.spyOn` from vitest (not jest) for:
    - Mocking global `fetch` in proxy.test.ts
    - Spying on `process.exit` in startup.test.ts and crypto.test.ts key-load tests
    - Mocking `node:fs` `existsSync`/`readFileSync` in key load tests

    Mock `process.exit` using: `vi.spyOn(process, 'exit').mockImplementation(() => { throw new Error('process.exit called') })`
    Then assert: `expect(() => loadEncryptionKey()).toThrow('process.exit called')`

    Import paths must use the `@/` alias (e.g., `import { encrypt, decrypt } from '@/lib/crypto'`).

    Do NOT use `describe` blocks — flat test structure with `test('...', () => {})` is fine.

    After writing: run `cd /home/cf/.openclaw/workspace/apps/automations/server && npx vitest run --reporter=dot 2>&1` and confirm all tests FAIL (not error in config). If vitest itself errors on config, fix that first.
  </action>
  <verify>
    <automated>cd /home/cf/.openclaw/workspace/apps/automations/server && npx vitest run --reporter=dot 2>&1 | tail -10</automated>
  </verify>
  <done>All 4 test files exist, vitest reports them as FAILING (red) with import/module errors — NOT config errors. The number of failing tests is >= 14 (covering all behaviors above).</done>
</task>

</tasks>

<verification>
After Wave 0:
- `ls server/tests/` shows crypto.test.ts, proxy.test.ts, execute.test.ts, startup.test.ts
- `ls server/vitest.config.ts` exists
- `cd server && npx vitest run` exits non-zero (tests fail — expected) but vitest RUNS without configuration errors
- Test count >= 14 failing tests
</verification>

<success_criteria>
Test scaffold is in place. All subsequent implementation tasks in this phase can use `cd server && npx vitest run --reporter=dot` as their automated verify command pointing at real files. Tests fail now and will turn green as implementation progresses.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
